version: "2"

sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        out: "database"
          #        overrides:
          #          - column: messages.created_at
          #            go_type:
          #              type: Time
